# Database Configuration
# ⚠️  변경 필요: 로컬 개발 시에만 이 값 사용, 프로덕션에서는 안전한 값으로 변경
DB_HOST=localhost
DB_PORT=5432
DB_NAME=starter_db
DB_USER=starter_user
DB_PASSWORD=starter_password

# JWT Configuration
# ⚠️  필수: 프로덕션 환경에서는 안전한 256비트(32자 이상) 무작위 값으로 반드시 변경
JWT_SECRET=your-secret-key-at-least-32-characters-long-for-HS256

# Server Configuration
SERVER_PORT=8080
